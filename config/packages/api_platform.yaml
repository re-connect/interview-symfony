api_platform:
    mapping:
        paths: ['%kernel.project_dir%/src/Entity']
    patch_formats:
        json: ['application/merge-patch+json']
    defaults:
        pagination_items_per_page: 50
    swagger:
        versions: [3]
        api_keys:
            apiKey:
                name: Authorization
                type: header